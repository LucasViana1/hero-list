const red = { normal: '#ff1510', light: '#fdecec' };

const green = { light: '#e7f6e7' };

const gray = { dark: '#404040', normal: '#8c8c8c', light: '#b9b9b9' };

const color = { red, green, gray };

export default color;
